apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: metrics-app
  labels:
    app: metrics-app
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metrics-app
      component: backend
  template:
    metadata:
      labels:
        app: metrics-app
        component: backend
    spec:
      containers:
        - name: backend
          image: rrqs12/infilect-backend:a4b1d2a15bd487d30f67c2748a9dde83e729a943
          ports:
            - containerPort: 5000
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
